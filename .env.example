# OPENAI_API_KEY: Required for OpenAI API calls.
# Configure this locally in .env.local or inject it through your hosting platform.
OPENAI_API_KEY="MY_OPENAI_API_KEY"

# Optional OpenAI model override for the Responses API.
OPENAI_MODEL="gpt-4.1-mini"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Optional local port override.
PORT="8080"
